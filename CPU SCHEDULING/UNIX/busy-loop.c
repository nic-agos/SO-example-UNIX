// compile with -nostartfiles, execute with ./a.out & e utilizzo top da shell per vedere quali programmi sono in esecuzione
// utilizzo da shell renice -n value -p pid
void __start(){
	while(1);  //sono cpu intensive
}
